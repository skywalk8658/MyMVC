
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>

    <script src='http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js'></script>
</head>
<body>
    <h1>@ViewBag.Title</h1>
    <div id="main-content"> @RenderBody()  </div>
    <H3>第一個footer ，直接指定，因同一個layout無法同時載入兩次RenderSection("footer")，此程式已注解</H3>
    @*<footer>@RenderSection("footer")</footer>*@

   <h3>第二個footer，使用if判斷 IsSectionDefined 加入預設值，若無設定會顯示預設footer</h3>
    <footer>
        @if (IsSectionDefined("Footer"))
        {
            @RenderSection("Footer");
        }
        else
        {
            <span>這是Layout預設的footer，當使用Layout的View沒有提供，會顯示這段文字</span>
        }
    </footer>


    <p>
        <h2>使用jQuery的Ajax Load載入一個部份視圖如下</h2>
        <div id="result"></div>
        <script type="text/javascript">
         $(function () {
             $('#result').load('index72');
         });
        </script>
    </p>
</body>
</html>
